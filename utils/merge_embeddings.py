import os


def merge(embeddings_dir):
    if len(os.listdir(embeddings_dir)) == 0: raise Exception('please compute embeddings before clustering')
    


    #f_alpha = load_model(...)

    #boucle dec


    #tant que pas implemente on utilisera
    #embeddings = concatenate(z1, z2)
    #return embeddings